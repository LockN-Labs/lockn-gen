# Deploy Orchestrator Environment Configuration
# Copy to ~/.lockn/deploy.env or /etc/lockn/deploy.env

# Required: SSH connection to deploy host
DEPLOY_HOST=deploy@prod-host.example.com
DEPLOY_SSH_PORT=22
DEPLOY_SSH_OPTS="-i ~/.ssh/deploy_key"

# Docker Compose directory on deploy host
DEPLOY_COMPOSE_DIR=/srv/apps

# Caddy Admin API endpoint (on deploy host)
DEPLOY_CADDY_API=http://127.0.0.1:2019

# Health check timeout (seconds)
DEPLOY_TIMEOUT=60

# Slack notifications (optional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz
SLACK_CHANNEL=#deployments

# Container registry (for cron job)
REGISTRY_URL=ghcr.io/lockn-ai
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Log directory for cron job
DEPLOY_LOG_DIR=/var/log/lockn-deploy
