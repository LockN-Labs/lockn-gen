# 2026-02-10 — Score Demo Sprint & Linear Reorg

## Key Events
- **Figma component library built**: 9 components (Button×3, Card, Badge, Score×4, Input) with full variable binding to 56 design tokens
- **Linear workspace consolidated**: 30→21 projects, 9 duplicates archived, 7 initiatives wired (Sean created manually)
- **Ops TPM agent live**: Cron `881844c8`, daily 8AM ET, Deepseek, isolated session
- **Priority monitor cron**: `28bcd9e9`, every 30 min
- **Process doc created**: Linear doc `044bdea3` — priority queue, naming convention, Definition of Done

## Score Demo Progress
- Web Docker image built (TS fixes: stubs for StreamPreview/ReadyButton/JoinSession, framer-motion types, colors.accent→primary)
- score-web-test container started on test.lockn.ai
- score-api-test build in progress (session `tide-lobster`, pid 2187869) — heavy ML deps, previous attempt OOM'd
- Auth0 issue: `https://api.lockn.ai` API not registered → `access_denied` on test.lockn.ai/score
- Demo bypass: `VITE_DEMO_MODE_BYPASS_AUTH=true` in test compose

## Portal Fixes
- lockn-web: navbar with 3 CTAs, feature flags file (`d9c6104`)
- lockn-ai-platform: removed duplicate CTAs, renamed Auth tab (`8952161`)

## Issue Rename Progress
- ~14 renamed (8 in Score MVP, 6 verified in Score UX)
- ~200+ remaining across System Improvements, Auth, Infra, Marketing, etc.
- Approach: project-by-project batches of 50

## Architecture Notes (Score)
- WS `/ws/game/{session_id}`: binary frames 0x01+JPEG (video), 0x02+float32 PCM (audio)
- Audio pipeline: AudioStreamHandler → PANNsClassifier → bounce events (confidence ≥0.55) → rally tracker
- Rally end: fusion tracker `rally_timeout_ms: 2500`, `vision_timeout_ms: 500`
- Gap: audio-only mode has no auto-miss trigger (~3s silence should trigger)

## Next Steps
1. Monitor score-api-test build for OOM
2. Verify both score containers healthy
3. Smoke test Score + waitlist on test.lockn.ai
4. Register Auth0 API or confirm demo bypass works
5. Continue bulk issue rename
6. Figma: iPad Dashboard layout, remaining components
