# LOC-255 Landing Page v2 Implementation Plan\n\n## 1. Component Breakdown\n\n**Frontend (React):**\n- Public Landing Page (CTA + Waitlist Signup)\n- Auth0-protected Admin Dashboard\n- User Status Page (waitlist position/eligibility)\n\n**Backend (.NET):**\n- `POST /api/waitlist` (signup endpoint)\n- `GET /api/user/status` (check access tier)\n- `POST /api/admin/promote` (promote waitlist users)\n\n**Auth0 Configuration:**\n- Custom signup action to capture email\n- Rule to assign `waitlist-member` role\n- Metadata fields for `accessTier` and `waitlistPosition`\n\n## 2. Data Model\n\n```csharp\npublic class WaitlistEntry\n{\n    public string Email { get; set; }\n    public string Auth0UserId { get; set; }\n    public string Status { get; set; } // "pending", "beta", "early-access"\n    public DateTime RegisteredAt { get; set; }\n    public int Position { get; set; }\n}\n``\n\n## 3. Auth0 Integration\n\n1. **Signup Action**: Capture email during signup → store in waitlist DB\n2. **Role Assignment**:\n   - All waitlist users get `waitlist-member` role\n   - Promoted users get `beta-tester`/`early-access` roles\n3. **Metadata**: Store `accessTier` and `waitlistPosition` in user app_metadata\n\n## 4. Email Flow\n\n1. **Waitlist Confirmation** (Postmark template):\n   - Sent immediately after signup\n   - Includes estimated wait time\n\n2. **Promotion Notification** (Postmark template):\n   - Triggered when status changes to Beta\n   - Includes login link and access instructions\n\n## 5. Estimated Effort\n\n| Component | Effort |\n|---|---|\n| Backend API | 4h |\n| Auth0 Actions | 3h |\n| Frontend Pages | 6h |\n| Email Templates | 3h |\n| Admin Dashboard | 5h |\n\n## 6. Implementation Order\n1. Create waitlist DB model\n2. Implement signup endpoint\n3. Configure Auth0 actions/rules\n4. Build frontend signup form\n5. Set up Postmark templates\n6. Implement admin promotion interface\n\n## Open Questions\n1. Should we add email verification before adding to waitlist?\n2. What are the exact access control requirements for prod vs test environments?\n3. Are there existing Postmark templates we can reuse for emails?\n\n// TODO: Add diagram of Auth0 flow and data model relationships